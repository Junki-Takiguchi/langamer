<div>
  <h1 class="page-title">
    <% if current_user.id == @user.id %>
      マイページ
    <% else %>
      <%= @profile.account_name %>の詳細ページ
    <% end %>
  </h1>
</div>
<%= render 'user_show' %>
<% if @recruitments.present? %>
  <div>
    <h1 class="page-title">募集中の投稿</h1>
  </div>
<% end %>
<div class="container main-contents">
  <div class="row">
    <div class="col-md-3">
    </div>
    <div class="col-md-7 profile-field">
      <% @recruitments.each do |recruitment| %>
        <br />
        <div class="form_box">
          <div class="profile-content">
            <%= link_to recruitment.title, recruitment_path(recruitment.id), class: "recruitment-title content-padding" %>
          </div>
        </div>
        <br/>
      <% end %>
    </div>
    <div class="col-md-2 button-padding">
    </div>
  </div>
</div>
