<div class="main-image">
  <div class="container">
    <div class="row">
      <div class="col-md-7">
        <p id="logo">Langamer</p>
        <div id="explanation">
          <strong><p id ="explanation-title">Langamer とは…</p></strong>
          <em>ゲームを通して言語交流の友達を見つけるサービスです。<br />
          自分の話せる言語を学びたい相手を見つけて、<br />
          ゲームの世界を共有して話題を広げてみましょう！</em>
        </div>
      </div>
      <div class="col-md-2">
      </div>
      <div class="col-md-3 signup-form border contents">
        <h2 class="text-center">ログイン</h2>
        <% if notice.present? %>
          <p class="notice"><%= notice %></p>
        <% end %>
        <% if alert.present? %>
          <p class="alert"><%= alert %></p>
        <% end %>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.label :email, 'Eメール', class: "form-label" %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>
          <div class="field">
            <%= f.label :password, 'パスワード', class: "form-label" %><br />
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
          </div>
          <% if devise_mapping.rememberable? %>
            <div class="field">
              <%= f.check_box :remember_me, class: "form-check-input" %>
              <%= f.label :remember_me, '次回から自動的にログイン', class: "form-check-label" %>
            </div>
          <% end %>
          <div class="actions">
            <%= f.submit "ログイン", class: "btn btn-primary btn-lg" %>
          </div>
        <% end %>
        <br />
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.hidden_field :email, value: 'test01@gmail.com' %>
            <%= f.hidden_field :password, value: 'test01' %>
          </div>
          <div class="actions">
            <%= f.submit "ゲストログイン", class: "btn btn-secondary guest-login" %>
          </div>
        <% end %>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.hidden_field :email, value: 'admin01@gmail.com' %>
            <%= f.hidden_field :password, value: 'admin01' %>
          <div class="actions">
            <%= f.submit "ゲストログイン(admin)", class: "btn btn-secondary guest-login" %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
