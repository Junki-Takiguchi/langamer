/*.messageを部分更新。appendメソッドで要素を追加 */
$('.message').append('<tbody>\
                      <tr>\
                        <th></th>\
                        <th>\
                          <div class="post_message">\
                            <%= image_tag current_user.user_detail.profile_picture.url %>\
                            <%= current_user.user_detail.account_name %>\
                            <p style="text-align: right;"><%= @message.message %></p>\
                          </div>\
                        </th>\
                      </tr>\
                    </tbody>\
');

/*チャット投稿後、フォームの値に前回の投稿内容が残らないようにする */
$('input[type=text]').val("")
